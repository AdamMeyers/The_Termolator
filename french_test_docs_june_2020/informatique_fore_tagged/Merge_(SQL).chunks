Merge SQL La commande Merge en SQL
une instruction qui
<NP> un
plusieurs tuples
<NP> une table
vue
une base
données
données sources
des conditions
Cette commande
le standard SQL:2003
le standard
des combinaisons d’
instructions Insert
<NP> Update
Delete
la table
Une jointure droite
Right outer join
<NP> la cible
la source
plusieurs tuples
la source
un même tuple
la cible
une erreur
Conditions1
la condition
jointure
qui
un tuple cible
jour
la Conditions2
Cela
un tuple source
la condition
jointure
un tuple
la table
les instructions
WHEN MATCHED THEN
un tuple source
la condition
jointure
les instructions
WHEN NOT MATCHED THEN
Les données sources
une table
une vue
une
requête
